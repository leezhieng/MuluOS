"""MuluOS application manager ("Add or Remove Programs").

Lists installed bundles from the registry and uninstalls them via the
privileged helper /usr/libexec/muluos/app-uninstall (run under pkexec).
"""
