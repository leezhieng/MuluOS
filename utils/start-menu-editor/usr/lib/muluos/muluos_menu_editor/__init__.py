"""MuluOS Start Menu editor.

The editing UI lives in widget.MenuEditorWidget so it can be embedded both
in the standalone window (app.py) and in the MuluOS Settings panel. All data
goes through the SQLite-backed registry (muluos_registry -> registryd).
"""
